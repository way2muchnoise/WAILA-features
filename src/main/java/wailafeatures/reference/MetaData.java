package wailafeatures.reference;

import net.minecraftforge.fml.common.ModMetadata;

import java.util.Arrays;

/**
 * Holds the metadata
 */
public class MetaData
{
    /**
     * Setup mod metadata
     *
     * @param metadata
     * @return edited metadata object
     */
    public static ModMetadata init(ModMetadata metadata)
    {
        metadata.modId = Reference.ID;
        metadata.name = Reference.NAME;
        metadata.description = Reference.NAME + " adds some extra features to WAILA";
        //metadata.url = "";
        //metadata.logoFile = "assets/" + Reference.ID + "/logo.png";
        metadata.version = Reference.VERSION_FULL;
        metadata.authorList = Arrays.asList("way2muchnoise");
        //metadata.credits = "";
        metadata.autogenerated = false;
        return metadata;
    }
}
